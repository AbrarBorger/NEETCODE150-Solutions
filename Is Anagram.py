class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        # sum = sum1 = sum2 = 0
        # ascii1 = [ord(char) for char in s]
        # ascii2 = [ord(char) for char in t]

        # for i in ascii1:
        #     sum1 += i
        # for i in ascii2:
        #     sum2 += i

        # if sum1 == sum2:
        #     return True
        # else:
        #     return False
        
    
